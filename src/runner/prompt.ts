export const DEFAULT_PROMPT = `בדוק את העמודים הבאים (תמונה + טקסט מחולץ לכל עמוד) ומצא טעויות טכניות ברורות.

== כלל יסוד ==
התמונה היא האמת. הטקסט המחולץ הוא עזר חיפוש בלבד; הוא לא מהימן ועלול לכלול שיבושים שאינם טעויות בעמוד עצמו:
- סדר אותיות שגוי (למשל "אמ ה צעי" כשבעמוד כתוב "האמצעי")
- סוגריים בכיוון הפוך בגלל bidi/RTL (")...(" כשבעמוד נראה "(...)")
- רווחים מיותרים או חסרים שאינם בעמוד
- אותיות שהפונט לא מיפה

לפני שאתה מדווח על טעות — וודא שהיא נראית בתמונה. אם היא קיימת רק בטקסט המחולץ ולא נראית בתמונה, אל תדווח עליה.

== מה לחפש (רק כשנראה בתמונה) ==
1. מילים כפולות ברצף (אותה מילה פעמיים רצוף)
2. סימני פיסוק כפולים (,, או :: או ..)
3. אות חסרה או מיותרת באופן ברור במילה
4. טעויות ברצף מספרים או אותיות (א', ב', ג', ד', ד', ו' במקום א', ב', ג', ד', ה', ו')
5. סימני שאלה (?) שנותרו בטקסט כסימון לעורכים
6. מראי מקומות שגויים (פרק/פסוק/דף שגויים)
7. כפילויות - אותו טקסט מופיע פעמיים
8. שגיאות/חוסר במספרי עמודים
9. שגיאות בכותרות

== מה לא לדווח עליו ==
- דקדוק, סגנון, עקביות כתיב, או החלטות עריכה
- שיבושי חילוץ: סדר אותיות, כיוון סוגריים, רווחים שאינם בתמונה
- כל דבר שאינך רואה בבירור בתמונה

== פלט ==
אם אין טעויות ודאיות, החזר: []

אחרת, החזר JSON בלבד:
[{"page": <מספר עמוד>, "text": "הציטוט מהטקסט המחולץ שמכסה את אזור הטעות", "error": "תיאור קצר", "fix": "התיקון כפי שצריך להיראות בעמוד"}]

שים לב לגבי שדה "text":
- העתק מהטקסט המחולץ של אותו עמוד את הקטע שמכסה את אזור הטעות — מילה-במילה, כולל סוגריים ורווחים כפי שהם בטקסט המחולץ (גם אם משובשים). זה דרוש למערכת לאתר את הטעות בעמוד.
- אל "תתקן" את הציטוט כדי שיראה כמו בתמונה — אם בטקסט כתוב ")אות שלה(" העתק כך, גם אם בתמונה זה "(אות שלה)".
- אם המקום לא קיים כלל בטקסט המחולץ, דלג על הטעות.

שדה "fix" — כתוב כפי שצריך להיראות בעמוד (כמו בתמונה), לא כפי שמופיע בטקסט המחולץ.

מספר העמוד הוא המספר המקומי בתוך הבאצ' (1 עד {batch_size}), לא המספר המקורי.

הנה הערות קיימות: {existing_comments}
`;

function existingCommentsBlock(
  pageNums: number[],
  existing: Record<number, string[]>,
): string {
  const lines: string[] = [];
  for (const pageNum of pageNums) {
    const comments = existing[pageNum];
    if (!comments?.length) continue;
    const localPage = pageNums.indexOf(pageNum) + 1;
    for (const comment of comments) {
      lines.push(`עמוד ${localPage}: ${comment}`);
    }
  }
  return lines.length ? lines.join('\n') : 'אין';
}

export function buildPrompt(
  template: string,
  pageNums: number[],
  existing: Record<number, string[]>,
): string {
  return template
    .replaceAll('{batch_size}', String(pageNums.length))
    .replaceAll('{existing_comments}', existingCommentsBlock(pageNums, existing));
}
